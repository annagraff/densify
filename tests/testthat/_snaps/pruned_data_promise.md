# print.pruned_data_promise prints correct information

    Code
      print(promise)
    Output
      <promise: pruned data frame[3, 2]>
      i use `as.data.frame(promise)` or `as_tibble(promise)` to obtain the data frame

